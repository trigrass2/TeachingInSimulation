<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cas.sim.tis.mapper.PreparationResourceMapper">
	<resultMap id="BaseResultMap" type="com.cas.sim.tis.entity.PreparationResource">
		<id column="ID" jdbcType="INTEGER" property="id" />
		<result column="TYPE" jdbcType="INTEGER" property="type" />
		<result column="PID" jdbcType="INTEGER" property="preparationId" />
		<result column="RID" jdbcType="INTEGER" property="relationId" />
		<result column="CREATOR" jdbcType="VARCHAR" property="creator" />
		<result column="CREATE_DATE" jdbcType="TIMESTAMP" property="createDate" />
		<result column="UPDATER" jdbcType="VARCHAR" property="updater" />
		<result column="UPDATE_DATE" jdbcType="TIMESTAMP" property="updateDate" />
		<result column="DEL" jdbcType="INTEGER" property="del" />
	</resultMap>
	<resultMap id="ResultInfoMap" type="com.cas.sim.tis.vo.PreparationInfo">
		<id column="ID" jdbcType="INTEGER" property="id" />
		<result column="ICON" jdbcType="INTEGER" property="icon" />
		<result column="NAME" jdbcType="VARCHAR" property="name" />
	</resultMap>
	<select id="findResourcesByPreparationId" resultMap="ResultInfoMap">
		select * from
		(select r1.id,r.type as icon,r.name from (select * from preparation_resource where pid=#{pid} and del=0 and type=1) r1 left join resource r on r1.rid=r.id
		union
		select r2.id,8 as icon,e.name from (select * from preparation_resource where pid=#{pid} and del=0 and type=2) r2 left join elec_comp e on r2.rid=e.id
		union
		select r3.id,8 as icon,t.name from (select * from preparation_resource where pid=#{pid} and del=0 and type=3) r3 left join typical_case t on r3.rid=t.id)a
		order by a.id;
	</select>
</mapper>